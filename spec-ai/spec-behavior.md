# 產品行為規格 (Behavior Spec)

## Spec

### 1. Program 頁面：動作輸入
* **入口**：採用日曆 (Calendar) 方式讓使用者選擇日期。
  * 支援切換時間週期檢視（日、週、月、季、年）。
  * 點選特定日期後，可選擇動作、輸入組次數與重量，以紀錄訓練。
* **預設動作範本機制**：
  * 提供預設的動作範本，讓使用者點選後產生範本，再自行修改實際的組數、次數與重量。
* **動作與器材組合清單**：
  * 理想上提供涵蓋訓練動作與器材組合的清單（例如：槓鈴深蹲、SSB 後腳抬高蹲），供選取後填寫紀錄。

### 2. 首頁 (Home Page)
* **Performance Overview 圖表**：
  * **最佳紀錄 (PR / 3RM / 5RM) 顯示**：
    * 系統由使用者的訓練紀錄（包含組數、次數、重量）中，找出各反覆次數的**「最新紀錄」**與**「歷史最高紀錄」**。
    * 例如：3RM 顯示的是該使用者**「最新一次」**「次數為 3」的重量；同時也會紀錄並顯示該使用者從開始紀錄起，所有「次數為 3」的紀錄中的**「歷史最高重量」**，以便觀察是否停滯或進步。
  * **動態 Tab 顯示邏輯**：
    * PR / 3RM / 5RM 等 Tab 需根據「使用者是否曾有該反覆次數的訓練紀錄」來決定是否出現。
    * 例如：如果沒有 1RM 紀錄，但有 10 筆 3 次的 Squat 紀錄，則僅顯示 3RM Tab，不顯示 1RM Tab。
* **自訂要追蹤 / 顯示的動作圖表**：
  * 有被使用者設定為「追蹤」的動作，方會顯示於首頁總覽圖表中。
  * **預設行為**：預設隨機挑選 3 個「已有訓練紀錄」的動作顯示（若未滿 3 個，則有幾個顯示幾個）。
  * **動作選項顯示條件**：必須要有紀錄，且至少包含一筆 3次 / 5次 / 1次 的紀錄才能出現在設定清單中，否則不顯示讓使用者選擇。
  * **MVP 限制**：最多可選擇 **三種** 動作進行追蹤與顯示。
  * **趨勢狀態指示**：各動作圖表上方需顯示該動作的表現趨勢狀態，包含：`↗ Trending Up`, `↘ Trending Down`, `Stable（尬電，你真的有在練嗎）`。
* **Body Fat Percentage 圖表**：
  * **自訂顯示開關**：允許使用者自行決定是否要在首頁追蹤 / 顯示體脂率圖表。
  * **啟用條件**：至少需有一筆體脂率紀錄，否則該選項 Disabled。
  * **時間與單位控制**：圖表需能切換單位，且支援左右拖拉時間軸。
  * **趨勢狀態指示**：圖表上方需顯示體脂率狀態，包含：`↗ Trending Up`, `↘ Trending Down`, `Stable`。
  * **UI 風格**：狀態顯示之風格與文字需與 Performance Overview 保持一致。

  * **UI 風格**：狀態顯示之風格與文字需與 Performance Overview 保持一致。

### 3. 資料儲存與驗證模式
系統需支援三種資料儲存與驗證模式切換，以適應不同執行環境：
* **Client 端儲存 (Local 模式)**：
  * 資料純粹存放在使用者裝置的瀏覽器中（LocalStorage）。
  * 不進行任何遠端連線或身分驗證，適合無網路或純單機測試使用。
* **Backend 端儲存 - 一般驗證 (Remote 模式)**：
  * 透過一般網頁或瀏覽器開啟時的模式。
  * **裝置識別**：系統會自動為使用者裝置產生一組唯一識別碼（Device ID）並記錄於裝置上。每次操作皆透過此 ID 與後端溝通，藉此辨識該裝置/使用者，不需額外註冊即可擁有專屬的雲端資料紀錄。
* **Backend 端儲存 - LINE Mini App 驗證 (LIFF 模式)**：
  * 在 LINE 應用程式內執行時的模式。
  * 透過 LINE 官方提供的驗證機制 (`liff.getIDToken()`) 確認使用者身分。
  * 確保在 LINE 生態系中能與使用者的 LINE 帳號緊密綁定並獲得安全的資料存取。

---

## Need to Confirm

* **[使用者提出] 動作自定義輸入 (MVP 版本)**：雖然理想上有完整的「訓練動作 + 器材」清單供選擇，但動作變化太多，MVP 版本需確認是否先讓使用者「自己輸入動作名稱」即可？
* **[使用者提出] 「自訂追蹤動作」功能入口與呈現**：讓使用者選擇「要追蹤/顯示之動作」的功能該擺在哪一頁？UI 如何呈現才能保持簡約，並符合 Apple Human Interface Guidelines？
* **[使用者提出] 首頁追蹤圖表 UI 優化**：目前前端已有表現追蹤圖表的初步實作，需檢視並討論如何調整才能更簡約、更貼近 Apple Human Interface Guidelines 規範。
* **[使用者提出] 「自訂追蹤體脂率」功能入口與呈現**：讓使用者決定是否顯示體脂率的設定或開關，該擺在哪一頁？UI 該如何設計以符合 Apple Human Interface Guidelines？
* **[使用者與AI共同提出] 體脂率圖表合併評估確認**：既然 Performance Overview 其實已將訓練表現與體脂趨勢放在同一張圖表內，是否還需要將「Body Fat Percentage 圖表」獨立成另一個專屬顯示區塊？

---

## QA 紀錄

| 紀錄時間 | 問題 / 討論重點 | 結論 / 狀態 |
| :--- | :--- | :--- |
| `2026-03-08 13:11` | **使用者提問**：<br>整理並確立 Program 頁面（動作輸入）與首頁（表現總覽、體脂圖表）之初步行為規格，並分離出待確認項目 (Need to Confirm)。<br>主要包含：<br>1. MVP 動作是否開放自定義輸入？<br>2. 首頁自訂圖表與體脂開關之 UI 位置與風格設計是否符合 HIG？<br>3. 體脂圖表是否要獨立顯示？ | **AI 結論**：<br>初步將使用者的想法轉換為 Spec 與 Need to Confirm 清單，並建立於 `spec-behavior.md`。此為問題盤點與規格收斂，後續將針對 Need to Confirm 項目逐一討論細部實作方式與介面設計。 |
| `2026-03-08 13:33` | **使用者提問**：<br>調整 3RM/5RM 等紀錄的定義：不只算最高，而是以「最新一次」的該次數重量為主，並同時紀錄「歷史最高重量」，藉此了解是否停滯或進步。 | **AI 結論**：<br>已更新 Spec，明確定義 PR / 3RM / 5RM 將同時顯示並追蹤各次數之「最新紀錄」與「歷史最高紀錄」。 |
| `2026-03-09 10:55` | **使用者提問**：<br>補充影響資料存放方式的 Spec。系統需支援三種模式切換：Client 端存儲、Backend 端 (LINE Mini App 驗證)、以及 Backend 端 (一般驗證)。一般驗證至少必須能辨識使用者進入網頁所使用的 Device。 | **AI 結論**：<br>已參考 `spec-backend.md`，在 Spec 區塊新增了「3. 資料儲存與驗證模式」，定義了 Local 模式、LIFF 模式，以及具備 Device 辨識能力的 Remote 模式。 |
