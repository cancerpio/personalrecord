# 產品行為規格 (Behavior Spec)

## Spec

### 1. Program 頁面：動作輸入
* **入口**：採用日曆 (Calendar) 方式讓使用者選擇日期。
  * 支援切換時間週期檢視（日、週、月、季、年）。
  * 點選特定日期後，可選擇動作、輸入組次數與重量，以紀錄訓練。
* **預設動作範本機制**：
  * 提供預設的動作範本，讓使用者點選後產生範本，再自行修改實際的組數、次數與重量。
* **動作與器材組合清單 (MVP 混合模式)**：
  * **基礎預設清單**：系統內建最具代表性的核心動作（如：Squat 深蹲、Bench Press 臥推、Deadlift 硬舉...等）做為預設下拉選單。
  * **支援自訂新增**：使用者可在選單中選擇「新增自訂動作」，自行輸入未涵蓋的動作名稱（例如：SSB 後腳抬高蹲）。
  * **個人化紀錄儲存**：自訂動作一旦儲存，會寫入使用者的專屬選單歷史中。未來新增紀錄時可直接選取，避免大小寫或選字不同而造成的資料不一致，確保 PR 追蹤與趨勢圖表計算正確。

### 2. 首頁 (Home Page)
* **廣度綜覽 (Overview - Sparklines)**：
  * **位置與功能**：位於首頁最上方，提供輕量、快速的多動作表現總覽。
  * **呈現內容**：列出使用者設定追蹤的動作（MVP 最多 3 個）。每個動作顯示「動作名稱」、「當前最高紀錄（如 3RM 最大值）」，以及一條極簡的走向趨勢線（Sparkline）與趨勢箭頭（`↗`, `↘`, `→`）。
  * **目的**：讓使用者開啟 App 的第一眼，不需滾動或點擊，即可迅速掌握近期主要訓練動作的整體進退步狀態。
* **深度深掘 (Deep Dive - 統一疊合主圖表 / Hero Section)**：
  * **位置與地位**：位於 Sparklines 下方，為主視覺區塊 (Hero Section)。首頁僅保留此唯一一張大型詳細圖表，極致符合 Apple HIG 資訊集中的簡約精神。
  * **單一動作專注**：圖表一次僅顯示「單一動作」的表現趨勢，可透過圖表上方的選單（如：`Squat ▾`）快速切換不同的動作。
  * **雙 Y 軸疊合顯示 (Dual Y-Axis)**：
    * **左側 Y 軸**：顯示訓練重量（公斤/磅），繪製該動作的 PR / 3RM / 5RM 趨勢。
    * **右側 Y 軸**：顯示體脂肪率（百分比 %）。
  * **體脂疊加開關 (Body Fat Overlay)**：圖表區塊提供一個開關（Toggle），點擊後即可在當前的重量趨勢圖上，疊加上體脂肪率的曲線。讓使用者能在一張圖內直觀對比「體脂/體重變化」對「力量表現」的關聯影響。若無體脂資料，該開關將 Disable。
  * **最佳紀錄顯示**：同原設定，系統會顯示該動作各次數的「最新紀錄」與「歷史最高紀錄」。

### 3. 資料儲存與驗證模式
系統需支援三種資料儲存與驗證模式切換，以適應不同執行環境：
* **Client 端儲存 (Local 模式)**：
  * 資料純粹存放在使用者裝置的瀏覽器中（LocalStorage）。
  * 不進行任何遠端連線或身分驗證，適合無網路或純單機測試使用。
* **Backend 端儲存 - LINE Mini App 驗證 (LIFF 模式)**：
  * 在 LINE 應用程式內執行時的模式。
  * 透過 LINE 官方提供的驗證機制 (`liff.getIDToken()`) 確認使用者身分。
  * 確保在 LINE 生態系中能與使用者的 LINE 帳號緊密綁定並獲得安全的資料存取。

---

## Need to Confirm

* ~~**[使用者提出] 動作自定義輸入 (MVP 版本)**：雖然理想上有完整的「訓練動作 + 器材」清單供選擇，但動作變化太多，MVP 版本需確認是否先讓使用者「自己輸入動作名稱」即可？~~ (已確認：採用「基礎預設清單 + 記住使用者的自訂動作」混合模式)
* ~~**[使用者提出] 「自訂追蹤動作」功能入口與呈現**：讓使用者選擇「要追蹤/顯示之動作」的功能該擺在哪一頁？UI 如何呈現才能保持簡約，並符合 Apple Human Interface Guidelines？~~ (已確認：收納於首頁頂端 Sparklines)
* ~~**[使用者提出] 首頁追蹤圖表 UI 優化**：目前前端已有表現追蹤圖表的初步實作，需檢視並討論如何調整才能更簡約、更貼近 Apple Human Interface Guidelines 規範。~~ (已確認：採用單一 Hero Section 大圖表)
* ~~**[使用者提出] 「自訂追蹤體脂率」功能入口與呈現**：讓使用者決定是否顯示體脂率的設定或開關，該擺在哪一頁？UI 該如何設計以符合 Apple Human Interface Guidelines？~~ (已確認：轉為 Hero Chart 的疊加開關)
* ~~**[使用者與AI共同提出] 體脂率圖表合併評估確認**：既然 Performance Overview 其實已將訓練表現與體脂趨勢放在同一張圖表內，是否還需要將「Body Fat Percentage 圖表」獨立成另一個專屬顯示區塊？~~ (已確認：廢除獨立區塊，採用雙 Y 軸合體呈現)
* **[使用者提出] LINE Mini App 導覽列 (Navigation) 限制**：當作為 LINE Mini App 運行時，會失去原生瀏覽器的 Navbar (上一頁/下一頁)。UI 該如何設計補足導覽功能（例如：Bottom Tab Bar 或自製 Top Navbar），以確保使用者不會迷失或操作卡死？

---

## QA 紀錄

| 紀錄時間 | 問題 / 討論重點 | 結論 / 狀態 |
| :--- | :--- | :--- |
| `2026-03-08 13:11` | **使用者提問**：<br>整理並確立 Program 頁面（動作輸入）與首頁（表現總覽、體脂圖表）之初步行為規格，並分離出待確認項目 (Need to Confirm)。<br>主要包含：<br>1. MVP 動作是否開放自定義輸入？<br>2. 首頁自訂圖表與體脂開關之 UI 位置與風格設計是否符合 HIG？<br>3. 體脂圖表是否要獨立顯示？ | **AI 結論**：<br>初步將使用者的想法轉換為 Spec 與 Need to Confirm 清單，並建立於 `spec-behavior.md`。此為問題盤點與規格收斂，後續將針對 Need to Confirm 項目逐一討論細部實作方式與介面設計。 |
| `2026-03-08 13:33` | **使用者提問**：<br>調整 3RM/5RM 等紀錄的定義：不只算最高，而是以「最新一次」的該次數重量為主，並同時紀錄「歷史最高重量」，藉此了解是否停滯或進步。 | **AI 結論**：<br>已更新 Spec，明確定義 PR / 3RM / 5RM 將同時顯示並追蹤各次數之「最新紀錄」與「歷史最高紀錄」。 |
| `2026-03-09 10:55` | **使用者提問**：<br>補充影響資料存放方式的 Spec。系統需支援三種模式切換：Client 端存儲、Backend 端 (LINE Mini App 驗證)、以及 Backend 端 (一般驗證)。一般驗證至少必須能辨識使用者進入網頁所使用的 Device。 | **AI 結論**：<br>已於後端技術評估後，確認基於瀏覽器 UUID 快取的裝置辨識不可靠。經討論決議拔除不穩定的 Remote (一般驗證) 模式，確立整體架構僅保留 `local` 與 `liff` 雙軌模式。 |
| `2026-03-09 11:55` | **使用者提問**：<br>討論首頁 UI 與體脂圖表的呈現方式。確認是否需保留獨立的體脂圖表，以及如何處理動作切換功能。 | **AI 結論**：<br>確立首頁 UI 採用「廣度綜覽 (Sparklines) + 深度深掘 (Hero Section 大圖表)」的雙層架構。廢除獨立體脂圖表，改為在主圖表疊加副 Y 軸顯示。藉此一併解決了所有 UI 與 HIG 相關的 "Need to Confirm" 項目。 |
| `2026-03-09 12:09` | **使用者提問**：<br>指出單一圖表的設計目前僅為「試作 (PoC)」，尚未最後 Confirm (需等待實作與試用後決定)。 | **AI 結論**：<br>已於 Spec 復原 Need to Confirm 的未解決狀態，並加上「目前試作方案 (PoC)」的備註。待試作版完成且使用者確認好用後，再正式結案。 |
| `2026-03-09 12:27` | **使用者提問**：<br>試用 PoC 後，確認雙 Y 軸疊合的概念有用，指示將此方案標記為正式接受。同時提出三個 UI 強化需求：<br>1. 右 Y 軸標註對應色塊。<br>2. 左 Y 軸需加上明確單位 (KG)。<br>3. 需有地方 (如副標題) 說明雙 Y 軸代表的意義。 | **AI 結論**：<br>已於本文件中正式 Confirm 四項相關的研發議程。並將上述三點細部優化，轉化為 `implementation_plan.md` 的 Action Item。 |
| `2026-03-09 15:10` | **使用者提問**：<br>提出 LINE Mini App 的導覽介面限制問題：當在 Mini App 內運行時，會失去原生瀏覽器導覽列，該如何將導覽功能放上 Web App？ | **AI 結論**：<br>確認此限制為真。建議採用 Bottom Tab Bar 與自製 Top Navbar 解決。已將此 UI 導覽架構議題放入 Need to Confirm 等待決策，為日後轉化為 `spec_and_tasks_mvp.md` SDD 先行準備。 |
