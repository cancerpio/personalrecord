# Feature TODO — 2026/08

依「重要性 + 可行性」排序的待辦功能清單。可行性以 ★ 表示（★★★★★ = 最容易落地）。

> **2026-09-05 更新**：#24 已合併（PR #70）。#21 的錯誤資料已由使用者處理並查證確認，但該項**未完成**——設計問題（最大重量對單筆錯誤零抵抗力）仍在。並更正 #21 中「使用者以重新輸入方式更正」的推論，該推論證據不足。
>
> **2026-08-31 追加 3**：新增 #25（計畫 / 已執行）。由 #24 的邊界條件討論分出——「放開未來日期」不是一個箭頭的設定，而是一個資料模型問題。
>
> **2026-08-31 追加 2**：#24 日期左右箭頭已實作待合併。`shiftDays` 由 `sessionStore` 搬至 `src/utils/date.js` 並補上單元測試。
>
> **2026-08-31 更新**：#18、#19、#22 皆已合併進 main（PR #65／#66／#68＋#69），狀態改為完成並移至表尾。#23 的前提（#22 上線）已滿足。
>
> **2026-08-31 追加**：新增 #22（動作總覽鑽取）、#23（Dashboard 區塊開關）、#24（日期左右箭頭）。
> 排名總表重排，「梯隊」欄更名為「優先序」並統一分級用語，已完成項目移至表尾。
>
> **2026-08-30 追加 2**：新增 #21（最大重量欄對單筆錯誤零抵抗力），內含一筆**尚未修正**的正式資料。
>
> **2026-08-30 追加**：新增 #20（Record 頁內嵌月曆）。同日的箭頭切換（±1 天）另行實作。
>
> **2026-08-29 追加 3**：新增 #18、#19（皆為使用中撞到的需求，已實作待合併），並附上首兩筆 audience-check 受眾標籤。
>
> **2026-08-29 追加 2**：#4 重寫——查證發現 feature branch 會部署到正式站；同時把 CI 的「界線內／界線外」明確寫下，後端自動部署改列為「決定暫緩」而非待辦。
>
> **2026-08-29 追加**：新增 #17（Firestore 安全規則納入版控）與文末的〈產品原則：護城河在哪裡〉。
>
> **2026-08-29 更新**：對照實際程式碼與 main 分支校正狀態欄——#11、#15 皆已合併進 main，先前仍標為未完成；#2 那一列的「重要性／可行性」欄位互換，已修正；#5 的白名單描述已與 `.mcp.json` 現況對齊。
>
> **2026-08-25 更新**：#16「最近動作連續週數」完成（#2 的核心計算已就緒）；同日稍晚修正其語意（錨點改為當週，詳見 #16）。
>
> **2026-08-23 更新**：新增 #11～#14 四個項目；把原本掛在 #2 底下的「動作名稱是自由文字」抽成獨立項目 #12，並依當天對正式資料的查證結果重寫該項的描述與做法；同時上修 #5 的可行性評估。
>
> **2026-08-23 追加**：新增 #15（Dashboard 當週平均體重趨勢顯示錯誤，已對正式資料查證確認為 bug）。

## 排名總表

「優先序」欄的分級：**現在**（有實際風險或已在手上）→ **待 review**（做完等確認）
→ **下一批** → **地基**（做完之後每件事都受益）→ **之後** → **隨時**（成本近零）→ **最後**。

| # | Feature | 重要性 | 可行性 | 優先序 | 狀態 |
|---|---|---|---|---|---|
| 17 | Firestore 安全規則納入版控（security） | 高 | ★★★★★ | 現在（可能正敞開著） | [ ] |
| 2 | 動作變化通知（黃/紅燈） | 中高 | ★★★★★ | 下一批（先定規則） | [ ] |
| 21 | 最大重量對單筆錯誤資料零抵抗力 | 中 | ★★★★ | 下一批（另有一筆待修資料） | [ ] |
| 12 | 動作身分一致性 | 中高 | ★★★★ | 下一批（#13、#2 的前置） | [ ] |
| 13 | 動作注意事項提示 | 中高 | ★★★★ | 下一批（與 #2 一起設計 UI） | [ ] |
| 20 | Record 頁內嵌月曆（標示有紀錄的日期） | 中高 | ★★★ | 之後（預設收合或展開未決） | [ ] |
| 23 | Dashboard 區塊開關（版面隨訓練週期調整） | 中 | ★★★ | 之後（#22 已上線，等實際使用回饋） | [ ] |
| 25 | 計畫 / 已執行（預先安排課表的資料模型） | 中 | ★★ | 之後（需先有 #4 後端測試） | [ ] |
| 3 | 撈出訓練資料的 API / 匯出 | 中高 | ★★★★ | 地基 | [ ] |
| 10 | 前端元件測試基礎設施 | 中 | ★★★★ | 地基（多項改動因它而無法測試） | [ ] |
| 4 | CI 收斂到界線內（4a 完成／4b 暫緩） | 中高 | ★★★★★ | 地基 | [x] 4a 完成 |
| 5 | MCP | 中 | ★★★★★ | 之後（唯讀已可用，剩語意層） | [ ] 部分完成 |
| 14 | 訓練筆記 | 中 | ★★★ | 之後（#4 之後） | [ ] |
| 7 | AI chatbot 檢視 / 優化課表 | 高 | ★★ | 之後 | [ ] |
| 8 | 紀錄前一天吃啥 | 低中 | ★★★ | 之後 | [ ] |
| 6 | SKILLS | 低（成本近零） | ★★★★★ | 隨時 | [ ] |
| 9 | 跟好友比賽訓練量和身體組成 | 中 | ★ | 最後 | [ ] |
| 24 | Record 頁日期左右箭頭（±1 天） | 中 | ★★★★★ | — | [x] 完成 |
| 22 | 動作總覽可點擊鑽取，移除 Sparkline | 中高 | ★★★★★ | — | [x] 完成 |
| 18 | 最近動作總覽加上訓練量與最大重量 | 中高 | ★★★★★ | — | [x] 完成 |
| 19 | Record 頁單一日期 | 中 | ★★★★★ | — | [x] 完成 |
| 15 | 當週平均體重趨勢顯示錯誤（bug） | 高 | ★★★★★ | — | [x] 完成 |
| 1 | 選動作時記得上次的組次數 | 中高 | ★★★★★ | — | [x] 完成 |
| 11 | 選動作時記得上次選的動作 | 中高 | ★★★★★ | — | [x] 完成 |
| 16 | 最近動作連續週數 | 中高 | ★★★★ | — | [x] 完成 |

---

## 1. 選動作時記得上次的組次數

**為什麼排第一**：投報率碾壓其他項。資料已在 store 裡，零後端、零 schema 變更，半天內完成，且是每次進健身房都會走到的路徑。

**做法**
- `src/stores/sessionStore.js` 加 getter `getLastSetForExercise(name)`：取該動作最近一次紀錄。
- `src/views/RecordView.vue`（`SearchableDropdown`，約 L233）選完動作後 prefill `form.weight` / `form.reps`。

---

## 11. 選動作時記得上次選的動作 ✅ 完成（2026-08-23，PR #56）

**這是 #1 的最後一哩。** #1 解決的是「選完動作後不用打數字」，#11 解決的是「連動作都不用選」。兩個加起來，打開記錄頁時表單已經是上次的最後一組，確認一下就能送出。單獨看 #11 價值普通，接在 #1 後面價值才完整。

### 資料從哪裡來

有兩種做法，**建議 (a)**：

- **(a) 從 `sessions` 的最後一筆紀錄取出 `exercise`**
  零新增儲存、零 schema 變更，而且跟 #1 用的是同一份資料、同一套「最後一組」判定邏輯（`getLastSetForExercise` 已經把 `date` / `createdAt` / 缺欄位的排序規則都釘死了，直接複用即可）。後端儲存模式下換裝置也一致。
- (b) 用 localStorage 記住上次在下拉選單選過的動作
  好處是「選了但沒送出」也記得，代價是多出一份跟 `sessions` 不同步的狀態，而且換裝置就失效。多一份狀態只換到一個邊緣情境，不划算。

### 實作上要注意的兩件事

**1. 載入時序**

`sessions` 是在 `onMounted` 才非同步抓回來的（`RecordView.vue` 的 `fetchSessions()`），元件掛載當下 `sessions` 還是空陣列。所以不能在 `onMounted` 裡直接設定 `form.exercise`，必須等資料載入完成後才設定。這是整個功能唯一真正需要小心的地方。

**2. 與既有 spec 衝突，屬於 MODIFIED 而非新增**

`openspec/specs/record-form-prefill/spec.md` 的 `Requirement: Prefill Triggers Only On Exercise Change` 明文寫著「頁面初次載入時所選動作為空，系統 SHALL NOT 因此清空任何欄位」，並且有一條對應的 Scenario「初次載入不清空欄位」。

#11 會讓初次載入時 `form.exercise` 從空字串變成某個動作名稱，進而觸發既有的 watch 帶入重量與次數 —— 行為正是我們要的，但它與上面那條規定直接牴觸。**開工時必須一併修改該 spec，這是 `MODIFIED: record-form-prefill`，不能當作沒看到。**

### 一個要先拍板的產品問題

整張表單（動作 + 重量 + 次數）全部預填、日期又預設今天之後，有可能手滑直接送出一筆不打算記的紀錄。三個選項：

1. **接受**（建議）—— 這個 app 是自己用、誤記可以刪掉，多一次點擊反而毀掉整個功能的意義。
2. 不自動填入，只在動作欄位下方顯示「上次：Barbell Overhead Press 50×5」，點一下才套用。
3. 只在「今天已經有紀錄」時才自動帶入（代表今天正在練、要接續記錄），否則留空。

### 不受 #12 影響

**#11 完全不受動作名稱分岔問題（#12）影響，不需要等 #12。** 因為 #11 是把最後一筆紀錄的 `exercise` 字串直接取出來再拿去精確比對，那個字串本來就來自資料本身，一定命中。名稱有沒有分岔跟這個功能無關。

---

## 16. 最近動作連續週數 ✅ 完成（2026-08-25）

在 Dashboard 的 12 週容積長條圖下方顯示一張表，列出每個動作「**目前**連續幾週沒換過」。
**只顯示，不通知。**

> **2026-08-25 稍晚語意修正**：初版把錨點設在「該動作最後練到的那一週」，
> 結果三個月前停練的動作照樣顯示「連續 8 週」——回答的是歷史，不是現況。
> 已改為**以當週為錨點**：當週沒練但上週有練仍持續，連續兩週沒練即歸零。
> 同時取消 12 週視窗過濾（**歸零的動作照常顯示**，中斷本身就是資訊），
> 改以**至多 12 列**限制長度，連續週數**上限 12**。

**為什麼先做這個而不是直接做 #2**：#2 的失效是靜默的——它的正常狀態就是「什麼都不顯示」，
壞掉時畫面跟正常時一模一樣。一個永遠顯示數字的畫面把它變成可觀察的。

這個判斷在設計階段就得到驗證：光是為了評估這個提案、用真實資料算了一次，
就發現 #2 的原始規則做出來會是壞的（見上方 #2 的更新說明）。若直接做通知，
這個問題不會被發現——因為壞掉的樣子就是「沒有通知」。

**產出**
- `sessionStore.exerciseStreaks` getter（16 個測試）
- `src/components/ExerciseStreakList.vue`
- `openspec/specs/exercise-streak-overview/spec.md`
- 設計文件 `docs/superpowers/specs/2026-08-25-exercise-streak-overview-design.md`
- 實作計畫 `docs/superpowers/plans/2026-08-25-exercise-streak-overview.md`

**刻意不做**：別名合併（屬 #12——讓名稱分岔直接顯示在畫面上）、
重量變化欄（產品意圖是結構性負荷輪替，不是偵測停滯）、通知（屬 #2）。

---

## 2. 動作變化通知（動作持續週數 → 黃燈 / 紅燈）

> **2026-08-25 更新：核心計算已由 #16 完成。** `sessionStore.exerciseStreaks` 已實作並有
> 16 個測試覆蓋，本項目剩下的只是「閾值判斷 + 通知 UI」。
>
> 注意 #16 後來把錨點改成當週、週數上限 12。對 #2 是好事——通知要的正是「現在還持續著」，
> 舊語意會對早已停練的動作發通知。但閾值設計需知道數字**不會超過 12**。
>
> 另外，#16 用真實資料驗證後推翻了本項原始需求的兩條規則，開工前務必先看 #16 的結論：
> - **「該週有練才算連續」太嚴格** —— 該定義下最高只有 3，而紅燈門檻是 4，通知永遠不會響。
>   已改為「每次最多容許連空一週」。
> - **不能從本週往回數** —— 週中查看時幾乎所有動作都會是 0。錨點必須是該動作最後練到的那一週。
>
> 建議累積數週實際使用經驗、確認 3/4 這兩個閾值合不合適之後，再決定要不要做通知。

**原始需求**
- 動作持續週數連續 **3 週**沒變 → 黃燈提示，並顯示該動作。
- 連續 **4 週**沒變 → 紅燈提示，並顯示該動作。
- 動作持續週數定義：
  - 計算起始日：跟訓練量一樣，從週日開始算。
  - 同一動作在一週內出現 2 次 → 該動作持續週數 +1。
  - 同一動作間隔 5 天未出現 → 不算連續，持續週數不加。
  - 同一動作間隔 10 天未出現 → 持續週數歸 0。

**目的（產品洞察）**
加入「變化動作」的概念。同一動作一直不換，容易讓該動作用到的某些肌群疲乏；換成變化動作可以解決這個問題並讓訓練持續下去。
例：持續不換的高背槓深蹲課表可能讓脊椎壓力過大、恢復不良；隔一段時間換成分腿蹲，可緩解脊椎壓力並持續給腿部足夠訓練量，下次換回高背槓深蹲時理論上能保留分腿蹲的好處，同時在脊椎壓力釋放的情況下取得進步。

**可以復用的資產**
- `src/components/CycleStatus.vue` 已寫好黃/紅燈 UI（`warning` / `critical` 樣式），目前只掛在 `src/App.vue.bak`、未被使用，可直接改造復用。
- `sessions` 已有 `date` + `exercise` 欄位，純前端 getter 即可算出。

**⚠️ 開工前必須先釐清的問題**

1. **週邊界與既有 spec 衝突**：需求寫「從週日開始算」，但這不是模糊地帶 —— `openspec/specs/weekly-training-volume/spec.md` 的 `Requirement: Weekly Grouping Boundary` 已明確規定「訓練週的範圍為**週一（起）至週日（迄）**，週日 SHALL 歸入其前一個週一所開始的那一週」，實作為 `getMondayOfDate()`（`src/stores/sessionStore.js:4`）。兩種處理方式：
   - **(建議) 沿用現況**：本功能直接複用週一起算的邊界，與訓練量一致，不需動既有程式。
   - **改成週日起算**：這是獨立的一件事，SHALL 另開一個 OpenSpec change（`MODIFIED: weekly-training-volume`），會動到當週容積、12 週序列、每週平均體重與整組測試。**不要混進本功能順手做**（見下方「衍生候選項」）。

2. **三條規則會互相打架**：「一週出現 2 次 +1」是以**週**為掃描單位，「隔 5 天 / 隔 10 天」是以**相鄰兩次間隔**為單位，兩種算法對同一份資料會給出不同答案。待決：
   - 某週出現 2 次但兩次相隔 6 天 → 算 +1 還是斷連續？
   - 某週只出現 1 次 → 不加、還是歸零？
   - 最終採「以週掃描、間隔天數只當打斷條件」還是反過來？

3. **「同一個動作」怎麼判定** → **已抽成獨立項目 #12**，是本功能的前置。原本這裡寫的是「動作名稱是自由文字，`Squat` / `squat` 會被當成不同動作」，但 2026-08-23 對正式資料的查證顯示那個描述是錯的（詳見 #12）。真正的問題不是大小寫或空白，而是同一個動作有多個合法名稱。

**建議做法**：把「持續週數」抽成 pure function，用 TDD 把上述邊界 case 一條條釘死，再接 UI。

**流程定位**：本項目前只是 backlog，規則未定案。依本 repo 既有節奏，開工時走 superpowers brainstorming → `docs/superpowers/specs/<date>-exercise-variation-alert-design.md` → `plans/<date>-*.md` → 實作時一併同步 `openspec/specs/`。定案後會是一個 **New Capability**（暫名 `exercise-variation-alert`）。

**衍生候選項（尚未決定要不要做）**
- [ ] 將訓練週邊界由「週一起算」改為「週日起算」 —— 獨立的 OpenSpec change，`MODIFIED: weekly-training-volume`，影響既有全部容積與體重計算。

---

## 12. 動作身分一致性

> 本項原本是 #2 底下的「待釐清問題 3」。2026-08-23 實際查過正式資料後發現：問題的性質跟原本以為的不一樣，而且它同時卡住 #2 和 #13 兩個功能，所以抽成獨立項目。

### 問題是什麼

整個 app 判斷「這兩筆紀錄是不是同一個動作」，用的就是字串完全相等 —— 沒有動作 ID、沒有動作對照表，`exercise` 欄位裡那個字串**就是**動作的身分。相關比對點有三處，都在 `src/stores/sessionStore.js`：

- `getSessionsByExercise`（L54）
- `getLastSetForExercise`（L69）
- `getChartSeriesForExercise`（L234）

只要同一個動作在資料裡有兩種寫法，這三處全部會把它當成兩個動作。

### 2026-08-23 的查證結果

**方法**：透過 Firebase 官方 MCP server（`firebase mcp`，白名單只開唯讀工具）讀取 Firestore 的 `training_sessions`，只取 `exercise` 與 `date` 兩個欄位。

> **2026-08-25 更正**：本節初版誤用「全量 432 筆」計算。`training_sessions` 是共用的
> top-level collection，其中只有 **373 筆**屬於本使用者（app 透過 `getSessions(userId)`
> 只會取到自己那些），其餘為另一帳號在 2026-03-21～04-02 的紀錄。以下數字皆已改為
> **加上 `userId` 過濾後的結果**。往後任何直接查 Firestore 的分析都必須加此條件。

**資料**：373 筆（已過濾 `userId`），期間 2026-04-03 ～ 2026-08-24。

#### 好消息：字串本身很乾淨

373 筆共 13 種名稱，以下分岔類型**全部零命中**：

| 檢查項目 | 結果 |
|---|---|
| 大小寫不一致（`Squat` / `squat`） | 0 |
| 頭尾空白、連續空白 | 0 |
| 非 ASCII、中英混用 | 0 |
| 缺 `exercise` 欄位 | 0 |

`SearchableDropdown.vue` 的 `addNewOption()` 會做 `trim()` 加每字首大寫，`canAdd` 也做了不分大小寫的重複檢查 —— 這套保護實際上擋得很好。

#### 壞消息：有另一種分岔，而且規則救不了

完整的名稱分布：

| 動作名稱 | 筆數 | 是否在內建清單 |
|---|---|---|
| Squat | 81 | ✓ |
| Bench Press | 64 | ✓ |
| Barbell Row | 46 | ✓ |
| **Overhead Press** | **40** | ✓ |
| Deadlift | 39 | ✓ |
| Hang Clean | 28 | ✗ |
| **Barbell Overhead Press** | **26** | ✗ |
| RDL | 18 | ✗ |
| Hang Snatch | 16 | ✗ |
| Split Squat | 8 | ✗ |
| **Dumbell Overhead Press** | **4** | ✗ |
| Pull Up | 2 | ✓ |
| Dumbbell Bench Press | 1 | ✗ |

> **2026-08-25 更新**：`Squat` 已全數正名為 `High Bar Squat`（105 筆，走 MCP 逐筆更新、零失敗），`constants.js` 同時新增 `Low Bar Squat`。當日重新全量統計為 **446 筆、13 種名稱**，除深蹲改名外，各名稱筆數變動如下：Bench Press 72、Barbell Row 63、Hang Clean 31、Hang Snatch 19、Split Squat 12；其餘不變。**分岔情形未改善**——`Overhead Press`(52) / `Barbell Overhead Press`(26) / `Dumbell Overhead Press`(4) 三者仍在，拼字錯誤也還在。
>
> 這次改名等於把 (c) 的資料清理路徑先跑過一遍：可行、可驗證、可回滾（備份在 `backend/backups/squat-rename-2026-08-25.json`）。剩下的肩推合併沿用同一套做法即可。

**肩推有三個名字，而且是來回交錯使用的**：

| 期間 | 使用的名稱 |
|---|---|
| 2026-04-03 ～ 05-24 | `Overhead Press` |
| 2026-05-26 ～ 05-31 | `Barbell Overhead Press` |
| 2026-06-04 ～ 07-05 | `Overhead Press` |
| 2026-07-16 | `Dumbell Overhead Press`（啞鈴，是不同動作） |
| 2026-07-31 ～ 08-23 | `Barbell Overhead Press` |

不是一次乾淨的改名，而是換過去又換回來、最後才定下來。最後一筆 `Barbell Overhead Press` 就是 08-23 當天記的。

另外 `Dumbell Overhead Press` 拼字錯誤（少一個 `b`），而同一份資料裡的 `Dumbbell Bench Press` 是拼對的 —— 同一個字在資料裡有兩種拼法。

#### 已確認：`Overhead Press` 就是 `Barbell Overhead Press`

經確認這兩個名稱指的是同一個動作，**統一採用 `Barbell Overhead Press`**（與啞鈴版對稱，也是目前實際在用的名稱）。

#### 不是分岔的部分

`Squat`(81) 與 `Split Squat`(8) 是真的兩個不同動作，不需要合併 —— 08-15 / 08-21 練分腿蹲、08-16 練深蹲，正是 #2 想鼓勵的「變化動作」行為，資料上看得出來已經在做了。`Bench Press` 與 `Dumbbell Bench Press` 同理。

#### 一個結構性問題：選單清單與歷史資料是兩份獨立的東西

這是讀程式碼就能確定的事實，不是推論：

| | 資料來源 | 內容 |
|---|---|---|
| 下拉選單顯示什麼 | `src/constants.js` 的 `BASE_EXERCISES`（8 個寫死的動作）＋ 該裝置 localStorage 的 `PR_CUSTOM_EXERCISES` | Squat, Bench Press, Deadlift, Overhead Press, Barbell Row, Pull Up, Chin Up, Dips（＋自訂） |
| 實際記錄了什麼 | Firestore 的 `training_sessions` | 373 筆、13 種動作 |

`SearchableDropdown.vue` 的 `allOptions` 是 `customOptions + props.options`，而 `RecordView.vue:243` 傳進去的是 `BASE_EXERCISES`。**`sessions` 從頭到尾沒有參與過。**

造成兩個後果：

1. **練過的動作不會出現在選單裡。** 有 101 筆、7 個動作不在內建清單上（Hang Clean 28、Barbell Overhead Press 26、RDL 18、Hang Snatch 16、Split Squat 8、Dumbell Overhead Press 4、Dumbbell Bench Press 1）。換一台裝置或清掉瀏覽器資料後，這些動作在選單裡就消失了，只能重打。
2. **防重複的檢查看不到歷史資料。** 打新名字時，`canAdd` 只拿那 8 個內建動作 + localStorage 比對，不會去看 373 筆紀錄。所以即使某個名稱已經用過 26 次，只要 localStorage 沒記著，它還是會跳出「Add "..."」，當成全新動作加進去。

反過來看，內建清單裡的 `Chin Up` 與 `Dips` 在 373 筆中一次都沒被使用過。

#### 查證的限制與推論

- **限制**：`PR_CUSTOM_EXERCISES` 是在桌面瀏覽器查到 `null` 的，但實際記錄主要在 LINE mini app（LIFF）進行，兩者的 localStorage 是不同儲存空間。因此**該數值不能代表 mini app 當下的自訂清單狀態**。
- **推論（未證實）**：分岔的成因很可能是「選單看不到歷史資料 → 被迫重打 → 打出不同名字」。這個推論合理但沒有直接證據，另一個可能是清單裡兩個名字都在、選的時候沒留意。**兩種成因的修法相同**，所以不影響下面的做法。
- **可以間接驗證的方式**：在 mini app 打開記錄頁，看下拉選單裡有沒有 `Hang Clean`、`RDL` 這些動作。有 → 清單是滿的，成因是選錯；沒有 → 清單是空的，成因是被迫重打。這會決定下面 (a) 的優先級是「治本」還是「順手改善」。

### 原本的假設被推翻了

本項目原本的計畫是「寫一個 `normalizeExerciseName()` 純函式，收合空白、統一大小寫」。**在實際資料上，那個方案一筆都改不到**，因為字串本來就乾淨。

`Overhead Press` 和 `Barbell Overhead Press` 都是合法、拼寫正確、格式乾淨的字串。任何 trim / 大小寫 / 空白規則都不會把它們合併。問題不是「字串髒掉」，而是**同一個動作有多個合法名稱**，需要的是別名處理與來源修正，不是字串正規化。

因此本項目由「動作名稱正規化」改名為「**動作身分一致性**」。

### 要做的三件事

**(a) 下拉選單的選項改從 `sessions` 推導**（治本，優先）

把 `allOptions` 從「`BASE_EXERCISES` + localStorage」改成「`BASE_EXERCISES` + **實際記錄過的動作** + localStorage」。改動很小，但一次解決三件事：換裝置看得到自己練過什麼、防重複檢查看得到真實資料、以後不會再產生新的分岔。

**(b) `src/constants.js` 的 `Overhead Press` 改名為 `Barbell Overhead Press`**

這一步不做的話前面會白費：就算把資料清理乾淨，內建清單裡的舊名稱還在，下次還是會選到它，馬上再分岔一次。可以順便檢討從未使用過的 `Chin Up` 與 `Dips`。

**(c) 一次性資料清理**（最多 44 筆）

| 項目 | 筆數 | 需要判斷嗎 |
|---|---|---|
| `Overhead Press` → `Barbell Overhead Press` | 40 | 否，已確認是同一動作 |
| `Dumbell Overhead Press` → `Dumbbell Overhead Press` | 4 | 否，純粹拼字錯誤 |

範圍比原本擔心的小很多 —— 是 44 筆（`Overhead Press` 40 筆 + `Dumbell` 拼字 4 筆），不是全部 373 筆。

**執行方式的建議**：暫時把 `firestore_update_document` 加進 `.mcp.json` 的白名單、做完後拿掉。每一次寫入都會經過 MCP 權限流程、看得到也可以拒絕。動手前先把這 44 筆的 docId 列出來存檔，萬一改錯可以照著還原。

**順序**：(a) 和 (b) 是純前端、無風險，應該先做；(c) 動到正式資料，可以晚一點單獨處理。先做 (a)(b) 的好處是，就算舊資料還沒清，至少不會再製造新的分岔。

### 為什麼是 #13 與 #2 的前置，而不是 #11 的

關鍵不在於「會不會出錯」，而在於**錯的方式是不是看得見的**：

| 功能 | 名稱分岔時會發生什麼 | 看得出來嗎 |
|---|---|---|
| #1 帶入上次組次數（已完成） | 帶不出來，表單留空 | 看得出來 —— 你知道自己練過 |
| #11 記住上次選的動作 | **不受影響**（字串直接取自資料本身） | 不適用 |
| **#2 動作變化通知** | 連續週數被打斷，該亮的紅燈不亮 | **看不出來** |
| **#13 動作注意事項提示** | 寫好的注意事項不跳出來 | **看不出來** |
| 歷史曲線 | 一條線斷成兩條 | 大致看得出來 |

#2 與 #13 是同一種病：**它們的正常狀態就是「什麼都不顯示」**。壞掉的時候畫面跟正常時一模一樣，沒有錯誤、沒有空白、沒有任何徵兆。你會以為「這禮拜沒有需要換的動作」，實際上是名稱對不上、判定根本沒跑到。而這兩個功能的價值完全建立在「該出現時一定會出現」—— 一個不能信任的提醒系統比沒有提醒更糟，因為你會依賴它。

#1 之所以能接受這個取捨並在 spec 裡明文放棄（`Requirement: Exact Exercise Name Matching`），正是因為它的失效是看得見的。

---

## 13. 動作注意事項提示

**需求**：在記錄某個動作時可以寫下注意事項，之後每次選到該動作，就把注意事項顯示出來提醒自己。例如「髖先走」「上次左肩有痛，暖身多做一組」。

### 必須和 #14 訓練筆記分開

這兩件事看起來相近，但資料模型根本不同，混在一起做會出問題：

| | #13 動作注意事項 | #14 訓練筆記 |
|---|---|---|
| 綁定對象 | 動作名稱 | 某一次訓練 |
| 性質 | 常駐設定，可編輯可刪 | 時序日記，只會累積 |
| 數量 | 每個動作 0～N 條，人為控制 | 隨時間無限增長 |
| 用途 | 被主動顯示出來 | 事後回顧 |

如果做成「筆記可以標記為重要 → 選動作時提示」，那提示會隨時間無限累積（練兩年的深蹲會有幾十條「重要筆記」），最後還得再做一套「顯示哪幾條」的邏輯來救。**一開始就分成 `exerciseTip`（常駐）與 `sessionNote`（時序）兩種資料，是這個功能最重要的設計決策。**

### 為什麼可行性高

資料結構很單純：一份 `{ [exerciseName]: string[] }`。選完動作後在下拉選單底下顯示一條 banner，加一個編輯入口就完成。半天到一天。

而且它不需要任何分析能力 —— 「髖先走」寫下去就是為了被看見，不像 #8「紀錄前一天吃啥」那樣需要結構化才有價值。

### 兩個要注意的地方

**1. 提示區塊要跟 #2 共用**

#2 動作變化通知也是「選動作時給你提醒」，而 `CycleStatus.vue` 的黃/紅燈樣式現在還沒被使用。如果 #13 各做各的，`RecordView` 會長出兩套互不相干的提示區塊。**建議 #13 的 banner 一開始就設計成能容納 #2 的燈號**，或乾脆兩者一起 brainstorm UI。

**2. #12 是前置條件**

提示綁在動作名稱上，名稱對不上就靜默失效（理由見 #12 末段）。

---

## 14. 訓練筆記

**需求**：可以記錄訓練筆記。

### 成本比 #13 高一階，主因有兩個

**1. 掛載層級還沒定案**

筆記要掛在哪一層？每一組？每一天？每個動作 × 每一天？三種選擇會導向完全不同的資料結構與 UI，這件事必須先想清楚。

**2. 要動 backend schema**

`backend/src/repositories/types.ts` 的 `TrainingSession` 加欄位，就要同時改 TS interface、Mongo 與 Firestore 兩個 repository 實作、以及 `LIFFService`。而這兩個 repository 目前**零測試覆蓋**（見 #4）。實質上這代表 #14 應該排在 #4 backend CI 之後再做比較安全。

### 價值評估

它會遭遇跟 #8「紀錄前一天吃啥」同樣的批評 —— 自由文字難以分析。差別在於：#13 的提示不需要被分析，只需要被顯示回來，所以價值立刻成立；而 #14 的分析價值要等 #5 MCP 或 #7 chatbot 才兌現，屬於「先存起來、以後才有用」的投資。

不過一旦 #5 做起來，訓練筆記會是餵給 AI 的高價值 context —— 「今天狀況不好、睡不飽」這類資訊是純數字資料完全沒有的。

**2026-08-30 補充**：若本項連帶把 app 帶往「預先安排課表」的方向（參考 `Mohup`），
則 #20 記錄的「日期不得往未來走」限制需一併重新評估。屆時的關鍵不是能否選未來日期，
而是資料模型要能區分「計畫」與「已執行」。

---

## 3. 撈出訓練資料的 API / 匯出

**現況**：後端已有 `GET /api/v1/sessions`（`backend/src/index.ts:93`），但綁死 LIFF token middleware；前端 local 模式資料只存在 localStorage。

**待決定要哪一種**
- (a) 個人備份／餵給 AI → 前端一顆「匯出 JSON/CSV」按鈕，約 1 小時。
- (b) 對外 read-only API → 需另做 API key 機制（LIFF token 無法給 script 使用）。

**排序理由**：原本認為它是 #5 MCP 與 #7 chatbot 的前置，但 #5 的前提在 2026-08-23 有變（見 #5）。目前它的價值主要是個人備份，建議先做 (a)。

---

## 4. CI 收斂到界線內（4a 做 / 4b 暫緩）

> **2026-08-29 重寫。** 原本只寫「backend 沒有 CI」，但查證後發現前端這塊也不是「已完成」——有一個部署設定的問題。同時把「要做到什麼程度」的界線明確寫下來，讓「不做」變成決定而不是待辦。

### 先修：feature branch 會部署到正式站（已查證）

`.github/workflows/deploy.yml` 的 trigger 是 `push: branches: [main, master, mini-app, "feat/**"]`，而 `deploy` job **沒有任何分支條件**。查 GitHub Deployments API 確認實際發生過：

```
2026-08-29T08:33:11Z  ref=feat/rename-squat-high-bar
2026-08-25T14:26:52Z  ref=feat/rename-squat-high-bar
2026-08-25T09:20:42Z  ref=feat/exercise-streak-overview
```

單人使用、且通常很快被 main 蓋掉，目前實際傷害小。但**分支放久就是長時間跑未 review 的版本**；而且若採用「agent 開 PR」的流程（見下方 #4a），等同**讓 agent 直接部署到正式站**。這一項要先於任何 agent 化工作。

### (a) 界線內：一次性做完就結束 ✅ 完成（2026-08-29）

改一個檔案（`.github/workflows/deploy.yml`）同時解決四件事——

- `on` 改為 `push: branches: [main]` **加上** `pull_request:`，讓測試在 PR 上跑。
  順帶移除已死的 `master` 與 `mini-app`（兩者最後 commit 都停在 2026-03-09，
  近 30 次部署中未曾出現）。
- `deploy` job 加條件：`if: github.ref == 'refs/heads/main' && github.event_name == 'push'`。
- `concurrency` 由固定的 `pages` 改為 `${{ github.workflow }}-${{ github.ref }}`。
  原本所有 ref 共用一組且 `cancel-in-progress: true`，PR 的 run 會取消掉
  正在進行的 main 部署。這是加上 `pull_request` trigger 後才會浮現的問題。
- `pages: write` / `id-token: write` 由 workflow 層下移到 `deploy` job，
  PR 的 run 不再持有部署權限。

**為什麼值得做**：把 review 從「這東西會不會動」變成「這個判斷對不對」——後者才需要人。而且沒有 PR gate 就無法在手機上 review，因為無從確認它能跑。這同時是 agent 開 PR 流程的前置。

**`openspec validate --specs` 決定不放進 CI**（2026-08-29）：`openspec` 是全域安裝的
CLI（`~/.nvm/.../bin/openspec` 1.5.0），不在 `package.json` 裡。要放進 CI 只有兩條路：
用未鎖版本的 `npx openspec`（每次 run 都上網抓、版本會漂），或新增一個 devDependency。
而它能擋的是 spec 檔的結構錯誤——只有在有人編輯 spec 時才可能發生，
而那個人本來就能在本機跑一次。為了低頻率、低嚴重度的檢查，在每次 CI 加上
一個未鎖版本的網路相依，不符合「界線內＝一次做完就結束」。
日後若 openspec 成為專案相依套件，再加不遲。

後續（同屬界線內，但可分開）：`backend/package.json` 補 test script、Firestore / Mongo 兩個 repository 實作補測試（目前零覆蓋）。#14 訓練筆記要動 backend schema，會直接受惠。

### (b) 界線外：明確不做

多環境 staging / prod、藍綠或 canary 部署、E2E 測試（元件測試基礎設施都還沒有，見 #10）、覆蓋率門檻（會變成為了數字寫測試）、semantic release / changelog 自動化、conventional commit 檢查、多 Node 版本 matrix、Dependabot auto-merge。

這些在有團隊、有 SLA 時都合理；在單一使用者的專案上是儀式。

**判斷標準**：CI 的價值在於「讓你敢按 merge」。不直接增加按下 merge 那一刻信心的東西，都在界線外。

### (c) 後端自動部署 — 暫緩，這是決定不是待辦

自動部署省下的只是「偶爾手動跑一次 `firebase deploy`」，換來的是一組 service account 金鑰躺在 GitHub secrets 裡（與 #17 同類的攻擊面）。PR 測試則是每次改動都受益。

**先做 (a) 就停。** 日後若手動部署真的造成困擾再回來評估。

---

## 10. 前端元件測試基礎設施

**緣起**：`record-form-prefill` 完成時，spec 中 12 個 Scenario 有 3 個沒有自動化測試覆蓋，因為它們是 `RecordView.vue` 裡 watch 的「接線行為」，store 層的單元測試碰不到：

| 未覆蓋的行為 | 目前靠什麼成立 |
|---|---|
| 初次載入不清空欄位 | watch 沒加 `{ immediate: true }` |
| 改日期不觸發帶入 | watch 監聽 `() => form.exercise` 而非整個 `form` |
| 送出後欄位保留 | `submitLog()` 沒有 reset 表單 |

三者都屬於「改一行就悄悄破掉、且不會報錯」的類型。例如有人在 `submitLog` 加了表單 reset，連續記多組的流程就沒了，但測試仍然全綠。

**要做的事**
- `vitest.config.js` 的 `environment` 由 `'node'` 改為 `'jsdom'`。
- 新增 devDependency：`@vue/test-utils`、`jsdom`（目前皆未安裝）。
- 處理 `RecordView.vue` 的測試 stub：`useRouter()`、`SearchableDropdown` 子元件、pinia store。
- 補上上表 3 個行為的測試。

**排序理由**：這 3 條行為本身不足以撐起一整套測試基礎設施，但「repo 從此能測 UI 行為」本身有價值，且愈早架成本愈低（目前只有 5 個 view/component 要照顧）。與 #4 同屬品質基礎設施，故並列於地基梯隊。刻意不併入 `record-form-prefill` 該次變更——那會讓一個小功能挾帶整個 repo 的測試策略決定。

**與 #11 的關聯**：#11「記住上次選的動作」同樣是 `RecordView.vue` 的接線行為（等資料載入完成後設定 `form.exercise`），一樣是 store 層測不到、壞了也不會報錯的類型。它會是這套基礎設施的第 4 個使用者。

---

## 5. MCP

> **2026-08-23 修正**：可行性由 ★★★ 上修為 ★★★★★，且 `#3 → #5` 的前置關係可能不成立。

原本的規劃是「做完 #3 資料匯出後，自己包一層 MCP server 把訓練資料暴露給 Claude」。但實際驗證後發現：**資料既然存在 Firestore，Firebase CLI 內建的官方 MCP server 就直接讀得到**，不需要自己寫。

- `firebase mcp` 是 firebase-tools 內建指令（已驗證於 15.11.0），提供 `firestore_list_documents`、`firestore_query_collection`、`firestore_run_aggregation_query` 等工具。
- 憑證直接沿用 `firebase login`，不需要 service account 金鑰。
- 本 repo 已在根目錄建立 `.mcp.json` 完成註冊，並用 `--tools` 白名單限制工具範圍。
  目前開四個唯讀工具，另加 `firestore_update_document`（2026-08-25 為 Squat 正名 migration 而加，決定長期保留）。
  **刪除工具仍排除在外。**
- 實際使用驗證：2026-08-25 的 Squat → High Bar Squat migration 全程走 MCP 完成（105 筆逐筆更新、零失敗），
  同日的全量時區稽核（446 筆）也是用 MCP 讀出來的。唯讀查詢與單筆更新這兩條路徑都已在正式資料上跑過。

因此 #5 的成本從「自己寫一個 MCP server」降到「加一個設定檔」，已經部分完成。

**剩下待評估的部分**：官方 MCP server 給的是通用的 Firestore 讀取能力，語意層級很低（要自己知道 collection 名稱、欄位結構）。如果希望 Claude 直接理解「這週的訓練容積」「這個動作的 PR」這類領域概念，仍然需要自己包一層薄的 MCP server 把 `sessionStore` 的 getter 邏輯暴露出來。那一層才需要 #3 當前置，而且要先確認確實有需要再做。

---

## 6. SKILLS

開發工具層、不是產品功能，所以重要性低；但成本近乎零（幾個 .md），repo 已有 `.claude/skills/` 慣例。想到就寫，不用排隊。

**已有**：`audience-check`（2026-08-29 建立）。在每次提需求／brainstorming 時多問五題——
需求是用出來還是想出來的、是否只解決我個人的問題、進階訓練者是否普遍會遇到、
一般使用者是否會遇到（推測＋最便宜的驗證方式）、以及這會不會讓 app 對我變差。

**定位是描述而非評分**：答「只有我」完全正常，不擋任何事、不影響優先順序。
這是為了在當下留下「這是誰的問題」的紀錄——n=1 的盲點無法靠思考修正，
但這個資訊事後重建不出來。累積之後會形成一份自己 app 的通用性地圖。

**記錄位置由開發流程決定，不由這個 skill 規定**（2026-08-31 修正，skill 原本越權寫了一張
位置對照表）。現行規則：完整答案寫進該需求的 design doc「受眾檢查」一節（**權威位置**，
長期保留），todo 對應項目加一行 `**受眾**：…` 作為可一次掃完的索引。
由於本檔是暫時性的收件匣，**受眾資料不得只存在這裡**。

`audience-check` 只在**會改變 app 對使用者行為**的需求觸發；安全性、CI/CD、基礎建設、
錯誤修正、資料清理、純文件不觸發（見 skill 的〈適用範圍〉）。

既有的 #1～#17 尚未回填受眾標籤（需要作者本人回答，不能代填），#18 之後皆已標註。
要不要回填是選擇性的——從新需求開始標，密度就足以累積出價值。

---

## 7. 讓 AI chatbot 幫忙檢視 / 優化課表

概念上最吸引人，但可行性最差：LLM key 管理、後端 proxy、成本、prompt 設計。

**注意**：第一版其實不需要 LLM —「連續 4 週沒換動作」「訓練量連 3 週下滑」這類規則式建議就能覆蓋八成價值，而那正好是 #2 的延伸。

另外，#5 的 MCP 路線已經可以讓 Claude 直接讀資料、給建議，成本遠低於自建 chatbot。除非需要「在 app 裡就能對話」這個體驗，否則 #7 的優先度應該持續往後放。

---

## 8. 紀錄前一天吃啥

新資料模型、新 UI、新的每日輸入習慣，但自由文字很難拿來分析，跟 `bodyMetrics` 也接不起來。除非結構化（熱量／蛋白質），否則做完只是個日記。

---

## 9. 跟好友比賽訓練量和身體組成

工程量最大。目前後端是純 per-user（`getSessions(userId)`），要加好友關係、授權、隱私設定、排行榜，等於一個新子系統，且對「自己好好訓練」這個核心價值幫助最不確定。

---

## 15. 當週平均體重趨勢顯示錯誤（bug） ✅ 完成（2026-08-23，PR #56）

**2026-08-23 發現、同日修正（commit `442329a`）。** Dashboard 標頭「當週平均體重」的 chip 顯示成 `-0.3 kg`，但當週體重其實是**上升**的。

> **修正結果**：兩個根因都處理了——(a) 比較基準由「全期完整週平均」改為**過去 12 個完整週平均**，(b) `bwTrendLabel` 改用 `utils/format.js` 的 `signed()` 保留正負號，不再靠 icon 表達方向。持平門檻同時放寬為 ±0.5 kg，`basis-note` 也把基準寫進畫面。以下保留當時的分析紀錄。

### 事實：實際資料長什麼樣

對正式資料（Firestore `body_metrics`，user `U55e5850...`）以 store 的原始函式重算：

| 週 | 平均體重 | 明細 |
|---|---|---|
| 8/10–8/16 | 81.080 kg | 81.6 / 80.9 / 80.6 / 80.6 / 81.7 |
| **8/17–8/23（當週）** | **81.150 kg** | 81.5 / 81.0 / 80.9 / 81.6 / 80.8 / 81.1 |

跟上一個完整週比是 **+0.07 kg，方向向上**。畫面顯示的 `-0.3` 在方向和數值上都不對。

### 根因：兩個獨立缺陷疊加

**(a) 比較基準是「全期」而不是近期**

[sessionStore.js:157-162](src/stores/sessionStore.js#L157-L162) 把當週拿去比「所有有紀錄的完整週」的平均——現在是 **17 週、一路回推到 4/6**：

```
avgBW = 80.879 (17 週)
delta = 81.150 - 80.879 = +0.271  →  toFixed(1) → 0.3
```

這個 0.3 既不是「跟上週比」，也不是「跟近期比」，而是「跟四個月平均比」。容積那欄至少還有一行 `趨勢基準：當週總量 vs 過往完整週平均` 說明，體重這欄完全沒標。

**(b) 「持平」的圖示長得跟負號一模一樣**

`BW_THRESHOLD = 0.3`，而 0.271 < 0.3 → `bodyWeightTrend = 'stable'`。[DashboardView.vue:370-373](src/views/DashboardView.vue#L370-L373) 的 `v-else` 分支畫的是一條水平線：

```html
<svg v-else><line x1="5" y1="12" x2="19" y2="12"/></svg>   <!-- 持平 -->
<span>{{ bwTrendLabel }}</span>                            <!-- "0.3 kg" -->
```

而 [DashboardView.vue:28-32](src/views/DashboardView.vue#L28-L32) 的 `bwTrendLabel` 用 `Math.abs()` 把正負號吃掉。水平線緊接著 `0.3 kg`，讀起來就是 `−0.3 kg`。**實際語意是「比長期平均高 0.27，在門檻內視為持平」，與畫面顯示的意思完全相反。**

### 做法

**1. 基準統一為「最近 12 個完整週（不含當週）」**

不能只改體重。目前同一張卡上已經有兩個互相不一致的平均：

| 位置 | 現況基準 | 程式碼 |
|---|---|---|
| 標頭 chip（容積 + 體重） | 所有有紀錄的完整週（17 週） | [sessionStore.js:115-131](src/stores/sessionStore.js#L115-L131) |
| 圖上虛線 /「過去 12 週平均」 | 最近 12 週 ÷ 12，含補 0 空白週、**含進行中的當週** | [sessionStore.js:224-226](src/stores/sessionStore.js#L224-L226) |

若只把體重改成 12 週，會變成「體重對齊圖表、但跟旁邊的容積 chip 對不上」，等於多生第三種基準。**三處一次收斂成同一個定義。**

圖上虛線也要一起改的理由：它現在把進行中的當週算進分母，等於拿當週去跟一個包含自己的平均比較，週初必然被自己拉低；而且 chip 講一個數字、虛線畫另一個數字，使用者對不起來。

**空白週的處理，體重與容積必須不同（這點刻意不對齊）：**

- 容積：空白週 = 那週沒訓練 = 真的 0，**補 0**，分母固定 12。
- 體重：空白週 = 那週沒量體重，**不可補 0**（會把平均拉到 70 幾）。只平均「視窗內有紀錄的週」，分母是實際有紀錄的週數。以目前資料是 10/12 週。

改完的數字（已用正式資料驗算）：

```
現況（全期 17 週）     avg 80.879  → delta +0.271 → 顯示 0.3、判定 stable
改為 12 週（10/12 有紀錄） avg 81.043  → delta +0.107 → 顯示 +0.1 kg
參考：跟上一完整週比                        +0.07
```

**2. 門檻由 ±0.3 kg 放寬到 ±0.5 kg**

`BW_THRESHOLD = 0.3` 設在雜訊底噪上。量測你自己的資料——相鄰兩個有紀錄的週之間，週平均體重的變化：

```
平均絕對變化 0.43 kg   中位數 0.27 kg   最大 1.98 kg
超過現行門檻 0.3 kg 的比例：47%
```

**接近一半的週間變動會踩過門檻**，等於 chip 有一半時間在報 up/down，訊號價值被稀釋。對 81 kg 的人來說 ±0.3 kg 還在水分／肝醣的範圍內。改為 **±0.5 kg**（約體重的 0.6%）。

**3. chip 與文案定案**

chip 是 `font-size: 12px`、`padding: 3px 9px`，且位於 `.stat-pair` 的 `1fr 1fr` 半欄內（手機實測可用寬度約 144px），**塞不下一整句話**。因此：完整語意放 `basis-note`，chip 只留精簡的帶號數字。

chip 三態（`delta` = 當週平均 − 過去 12 週平均，先四捨五入到小數一位後再判定）：

| 狀態 | 條件（r = 四捨五入後的 delta） | icon | chip 文字 |
|---|---|---|---|
| 高於基準 | `r > 0.5` | ↗ 箭頭（沿用） | `+0.6 kg` |
| 低於基準 | `r < -0.5` | ↘ 箭頭（沿用） | `-0.6 kg` |
| 門檻內 | `-0.5 ≤ r ≤ 0.5` | **● 實心圓點**（取代現行水平線） | `+0.1 kg` |
| 無紀錄 | 視窗內無體重資料 | 無 | `—` |

圓點的 SVG 要用 `fill="currentColor" stroke="none"`，不能沿用 chip 既有的 `fill="none" stroke="currentColor"` 寫法，否則會畫成空心圈。

`basis-note` 改寫（現行文字 `趨勢基準：當週總量 vs 過往完整週平均` 在基準改成 12 週後就不正確了，且體重欄從頭到尾沒有任何說明）：

```
趨勢基準：本週 vs 過去 12 個完整週平均（不含本週）；體重未達 ±0.5 kg 視為持平
```

**為什麼 basis-note 要寫「vs 過去 12 個完整週平均」而不是「上升／下降」**：跟平均比，回答的是「我相對這個區塊是高還是低」，不是「我正在變重還是變輕」。這兩件事不一樣。具體失效情境（用實際資料）——7/13→7/27 掉了 1.98 kg 之後已經穩在 80.5～81.1 五週，但 12 週視窗裡還躺著 6/29、7/06、7/13 三個 82.x 的週把基準墊到 81.043，所以現在只顯示 +0.1；**等那三週滾出視窗，基準粗估會掉到 80.7 左右，chip 會自己跳到 +0.4，但體重一克都沒變**。文案必須是相對語意，使用者才不會把它讀成方向。

（真正的「方向」統計量是最近 4～6 週的斜率，屬於另一個功能，不要混進這次修正。）

**4. 三個必須一起釘死的 edge case**

- **`-0.0`**：delta = −0.04 時 `toFixed(1)` 會產生 `-0.0`。必須**先四捨五入、再決定符號**：

  ```js
  const signed = (v, d = 1) => {
    const r = Number(v.toFixed(d));            // 先四捨五入
    const sign = r > 0 ? '+' : r < 0 ? '-' : ''; // r === 0 不加號
    return `${sign}${Math.abs(r).toFixed(d)}`;
  };
  ```

- **判定門檻與顯示精度必須同步**：舊版 delta 0.271 顯示成 `0.3`、門檻也是 0.3，卻判定 stable，畫面自相矛盾。**判定要吃 `signed()` 裡那個四捨五入後的 `r`**，不是原始浮點數。
- 視窗內完全沒有體重紀錄時 → `trend = 'none'` → chip 顯示 `—`（現有邏輯已涵蓋，但要在測試裡釘住）。

**5. 先寫失敗測試**

[src/stores/sessionStore.test.js](src/stores/sessionStore.test.js) 已有基礎設施。用 8/10–8/16 與 8/17–8/23 這組真實數字建 fixture，先讓「當週相對基準為正、且 chip 不得顯示負號」的測試失敗，再改實作。另外釘住：`-0.04 → "0.0"`（不得出現 `-0.0`）、`r = 0.5 → stable`、`r = 0.6 → up`、視窗內無紀錄 → `none`。

### 為什麼值得排在最前面

這不是體感問題，是**畫面上的數字與事實相反**。體重趨勢是拿來決定要不要調整飲食與訓練量的輸入，顯示反向會直接導致錯誤決策；而且修正範圍是純前端 getter 加 template，沒有 schema 或後端變更。

---

## 17. Firestore 安全規則納入版控（security）

**2026-08-29 提出。** 這是目前唯一一個「可能現在就是敞開的」風險，而且跟 repo 公開與否**完全無關**。

### 現況（已查證的部分）

- 前端**沒有**使用 Firebase SDK（`grep firebase src/` 零命中），所有資料存取都走 `LIFFService` → 後端 API。
- 後端 [backend/src/index.ts:40](backend/src/index.ts#L40) 的 `liffAuthMiddleware` 以 LINE token 換出 `userId`，每個 route 都以 `req.user.userId` 過濾。
- 資料庫憑證從未離開後端。**這個形狀是對的，不需要重做。**
- 但 `backend/firebase.json` **只宣告了 functions，沒有 firestore 區段**，repo 裡也沒有 `firestore.rules`。

### 待確認（我從 IDE 看不到，需人工查）

Firebase Console → Firestore → 規則。**如果還停在建立專案時的 test mode（`allow read, write: if true`），代表任何人拿到 project ID 就能讀寫整個資料庫。** 這與程式碼寫得多嚴謹無關，是繞過整個後端的一條路。

### 做法

- **(a)** 建立 `firestore.rules`，內容為**全部拒絕**客戶端存取。後端用的是 Admin SDK，會繞過 rules，所以功能完全不受影響——但任何直連 Firestore 的路徑會被關死。
- **(b)** 在 `backend/firebase.json` 加上 `firestore` 區段指向該檔，讓規則跟程式碼一起 review、一起部署。避免有人在 console 手滑改掉而沒人發現。
- **(c)** 部署後驗證：用未經授權的憑證嘗試直接讀 `training_sessions`，應被拒絕。

### 相關：轉 private 的前提

**git 歷史裡不能有金鑰。** repo 之後轉 private 不會讓已經公開過的東西消失——歷史一旦推出去就收不回來。轉 private 前應先掃過歷史。

順帶一提，目前留在 public 有一個實質好處：GitHub 對 public repo **免費提供 secret scanning 與 push protection**，會擋下帶金鑰的 push。轉 private 後這層保護需要付費方案（詳見〈產品原則〉一節關於可見性的討論）。

---

## 18. 最近動作總覽加上訓練量與最大重量

**受眾**：用出來的｜僅我：不知道｜進階者：不知道｜一般使用者：不知道｜對我變差：原設計會，改為合併後解除

> 狀態：**已完成**，2026-08-31 由 PR #66 合併進 main。

需要一個地方一目瞭然看到最近兩週練了哪些動作、各做多少組次數，以及該動作歷來最重。

### 為什麼是合併而不是新增一塊

原本規劃在 Dashboard 新增一個區塊。但 Dashboard 已經偏長（容積卡 → 動作表 → Sparklines → Performance Overview + 篩選器），而新表與既有的「最近動作連續週數」本質相同——都是以動作為列、都在回答「我最近在練什麼」。分成兩塊才是真正的冗餘。

合併後 Dashboard 不變長，資訊密度反而提高。代價是拿掉「最後練到」欄（仍用於排序，只是不顯示）——四欄是手機版面的上限，而連續週數已隱含近期性。

### 兩套時間範圍刻意不同

- **連續週數**走週界（週一起算），回答「這一週有沒有練到」
- **組數／總次數**走滾動 14 天，回答「最近兩週累積了多少」
- **最大重量**不受任何視窗限制，回答「這個動作我碰過最重多少」

### 命名：最大重量 ≠ PR

`getChartSeriesForExercise` 的 `PR` 是**嚴格 1RM**（`reps === 1` 的最大重量），註解寫明是刻意的。本功能的「最大重量」不限 `reps`。兩者分開命名，避免同一個詞在兩處有不同意義。原始需求本意是 1RM，但因為有些動作根本不會去做 1RM，改採不限 reps 的最大重量。

### 明確不做

不顯示「這 14 天有沒有破最大重量」。有價值，但屬另一項工作。

---

## 19. Record 頁單一日期

**受眾**：用出來的｜僅我：否，任何人用都會碰到｜一般使用者：同上，與是否為訓練者無關｜對我變差：未評估

> 狀態：**已完成**，2026-08-31 由 PR #65 合併進 main。

Record 頁原本有兩個獨立的 date input（體重一個、訓練一個），切換其中一個時另一個不動。而最下方的「已存紀錄」是依訓練那個日期過濾的，所以切上面的日期時連已存紀錄也不會跟著換。

改為單一日期控制項提到頁面最上方，三塊全部跟著它走。淨效果是**移除一個控制項**，而不是新增同步邏輯。

### 已知取捨：未儲存的體重輸入會被捨棄

在體重欄位打了數字還沒存、接著改日期，那個輸入會被該日期的紀錄覆寫或清空，且不提示。這是刻意的——本頁模型是「正在編輯某一天」，日期由使用者主動變更，欄位變化當場可見，且**不會產生錯誤資料**，只遺失可重打的輸入。

否決的兩個替代方案（完整理由見 `openspec/specs/record-form-prefill/spec.md` 的 Single Date Scope）：

- **「欄位被改過就不自動帶入」更危險**：欄位顯示今天的值、按鈕卻是 Update，按下去等於把今天的體重存進另一天——從遺失輸入惡化為產生錯誤資料。
- **「偵測未存修改並跳確認」**需在 `RecordView` 加有狀態邏輯，而該元件無任何自動化測試覆蓋（#10）。

日後實際使用反覆困擾時再重新評估，且須以元件測試覆蓋。

### 無自動化測試

RecordView 是 Vue 元件，前端元件測試基礎設施尚未建立（#10）。這是 #10 值得做的一個具體例子。

---

## 20. Record 頁內嵌月曆（標示有紀錄的日期）

**受眾**：用出來的｜僅我：不知道｜進階者：不知道｜一般使用者：不知道｜對我變差：需視預設狀態而定（見下方待決事項）

> 2026-08-30 討論，**尚未動工**。同日先實作的「日期左右箭頭（±1 天）」是另一件事，範圍小得多。

### 起因

Record 頁的日期要換到過去某一天，得點開原生日期選擇器，成本高。

### 查證：Record 頁事實上是唯一的單日檢視器

`src/router.js` 只有四個頁面。`SessionDetailView` 處理的是「某一天的**某個動作**」的編輯，
Dashboard 是彙總圖表，**沒有任何歷史瀏覽頁面**。要看「8/15 那天練了什麼、體重多少」，
唯一的路徑就是 Record 頁切日期。

### 真正的價值不是省一個 tap，是「看得出哪幾天有資料」

原生日期選擇器**無法標示哪些日期有紀錄**。使用者五個月內只有 51 個訓練日——
隨機點一天約有三分之二機率是空的，要找「上次練深蹲是哪天」只能一天一天盲試。

自製月曆可以在格子上標出有訓練紀錄／有體重紀錄的日子。這是**能力差距**，
不是便利性差距，也是本項值得做的主要理由。

### 待決事項：預設收合還是展開

- **收合**：記今天時不佔空間；要看過去時 1 tap 展開（與現在點開原生選擇器的 tap 數相同，
  但資訊多得多）。收合不會比現況差。
- **展開**：一打開就看得到整個月的分布，代價是體重、訓練表單、已存紀錄全部被推下去約 300px。
- **收合但記住上次狀態**（localStorage）：兩邊兼顧，代價是多一個要維護的狀態，
  而該元件無測試覆蓋。

取決於「檢視過去」與「記錄今天」的實際比例，而**這個比例資料裡量不到**（見下一節）。

### 資料佐證的範圍與限制（重要）

2026-08-30 查過 484 筆紀錄，比對每筆 `date` 與 `createdAt` 的本地日期：

| | 筆數 | 佔比 |
|---|---|---|
| 當天記錄 | 465 | 96.1% |
| 補記過去 | 19 | 3.9%（**全部都是前一天，無一超過 1 天**） |
| 記到未來 | 0 | 0% |

**這組數字只涵蓋「寫入」，看不到「瀏覽」。** 使用者切到過去某天只是「看」而不修改時，
不會留下任何痕跡。曾據此得出「從未跳超過一天，因此不需要月曆」的結論，
**那是錯的**——它回答的是編輯行為，不是檢視行為。日後若有人重新引用這組數字，
必須連同這個限制一起讀。

### 相關決定：日期不得往未來走（2026-08-30）

同日實作的箭頭切換中，「下一天」在 `selectedDate >= 今天` 時停用。
理由是本 app 是**紀錄工具**，不是計畫工具；資料中未來日期為 0 筆，
不擋的話連點兩下就會靜默記到明天。

**解開這個限制的條件**：使用者觀察到另一款 app `Mohup` 並不擋未來日期，
因為它的定位偏向**預先安排課表**。若本專案日後往「先排課表、再對照執行」的方向走
（與 #14 訓練筆記、#2 動作變化通知有關），這個限制就該重新評估——
屆時要擋的不是未來日期本身，而是要區分「計畫」與「已執行」兩種狀態。

### 範圍

- store getter：回傳「有訓練紀錄／有體重紀錄的日期集合」。純計算，**可完整 TDD**。
- 月曆元件：月份網格、跨月切換、標記。**無元件測試覆蓋**（見 #10）。

明顯大於箭頭那一項，故拆開處理。

---

## 21. 最大重量對單筆錯誤資料零抵抗力

**受眾**：尚未問（目前只是觀察，還沒成為需求）

> 2026-08-30 發現，資料部分已於 08-30 處理完畢（2026-09-05 查證確認）。
> **本項未完成**——待處理的是設計問題本身，不是那筆資料。

### 起因

使用者看到 Dashboard 的「最近動作總覽」顯示臥推最重 100 kg，但不記得推過 100。

### 查證結果

**划船 65 kg 是真的**：自己的帳號有 14 筆 65×5，分佈於 2026-04-12、04-23、05-10，
第二重 60 kg，差距僅 5 kg。只是五月後就沒再做，所以記不得。

**臥推 100 kg 幾乎確定是動作記錯**：

- 只有一筆：`2026-07-31 09:42:55, 100 × 3`，docId `GHkHyI3JeO9UJmevq8Oi`
- 第二重 75 kg，差 25 kg（33%）；2026-08-30 當天仍在做 75×1
- 那天是深蹲＋肩推＋划船日，**沒有任何其他臥推**
- 同一天的深蹲欄位出現過完全相同的 `100 × 3`（09:53:05）

掃過使用者全部 425 筆，以「最重 vs 第二重的差距」找異常，**只有這一筆可疑**。
深蹲 125（第二重 120）、硬舉 140（130）、RDL 80（75）、Hang Clean 70（65）皆連貫；
Hang Snatch 30 對 20 雖差 50%，但有 8 筆都是 30，是真的練上去。

### ✅ 資料部分已解決（2026-09-05 查證）

使用者於 2026-08-30 自行處理：刪除原本的 `GHkHyI3JeO9UJmevq8Oi`，
並以 `IcFWcWSRWYrK0S0yWpNG`（`2026-07-31 High Bar Squat 100×3`，建立於 08-30）重建，
因此那天的訓練容積不會少算。

查證方式：查詢 `weight >= 90` 的所有紀錄，**無任何一筆 Bench Press**。
Dashboard 的臥推最重已回到正確的 75。

**但本項不因此完成**——設計問題仍在，見下節。那筆資料只是觸發事件。

### 問題本身

`maxWeight` 取的是 `max(weight)`，**一筆打錯就永久污染該欄，而且畫面上完全看不出來**。
這次是靠使用者自己記得臥推沒到 100 才發現的——換成不熟悉的動作就不會被發現。

### 評估過的其他方向

以下三個屬「呈現層」的緩解，與最終選定的「輸入時驗證」互補，未來仍可考慮：

- **在最大重量旁同時顯示次數**：`100×3` 對比 `75×5`，異常一眼可疑。成本最低，
  但只幫助已經在看那一欄的人，不會主動找上你。
- **標示只出現過一次的最大值**：單次出現的極值本來就該被質疑。
- ~~改用「出現 ≥N 次的最大重量」~~ **已否決**（見下方〈業界怎麼做〉）：
  真正的單次 PR 恰恰是最該被看見的訊號，把它藏起來是拿有價值的資訊換抗噪。

### 順帶觀察

07-31 的 `09:53:05 深蹲 100×3` 與 `09:53:14 深蹲 80×5` 相隔 9 秒，
當時研判使用者更正的方式是重新輸入而非編輯既有紀錄。

**2026-09-05 更正**：這個推論證據不足。09-02 的 `QnsQJzvCiCPTxKhqpW5r`
（臥推 62×7）`createdAt` 11:59:54、`updatedAt` 12:00:01，相隔 7 秒——**是編輯**。
所以兩種更正方式都會發生。原推論只有單一資料點，不該當成行為模式。

不過結論方向不變：使用者確實會產生需要更正的紀錄，而 08-30 那次（刪除後重建）
證明錯誤紀錄也可能被刪除而非修改。設計即時通知時兩種路徑都要考慮。

### 業界怎麼做（2026-08-30 查證）

**PR 是主流訓練 App 的招牌功能，而且不只一種。** Hevy 同時追蹤：最重、
最佳 1RM（真實或估算）、單組最大容積、單次訓練該動作的最大總容積，
以及 **Set Records——在特定次數下的最大重量**（3 下最重多少、5 下最重多少……）。

**對「不會去做 1RM」的答案是估算 1RM（e1RM）**，用 Epley / Brzycki 之類的公式
從 weight × reps 推算，因此永遠不必真的試單次極限，每一組都貢獻資料。

**對「錯誤資料」的答案不是統計濾除，而是即時回饋。** Hevy 有 Live PR 通知，
存檔當下就跳「新紀錄」。本次的錯誤之所以躺了一個月，正是因為存檔時**毫無回饋**。

因此 todo 原先列的「用出現 ≥N 次的最大重量」**已否決**：那不是業界做法，
方向也不對——真正的單次 PR 恰恰是最該被看見的訊號，把它藏起來是拿有價值的資訊換抗噪。

### e1RM 對本專案有一個具體限制

估算公式假設的是可以 grind 的動作（蹲、推、拉），高次數（>10~12）失準。
而使用者練 **Hang Clean 與 Hang Snatch**——舉重動作受技術與速度限制，
不是力量極限限制，e1RM 會產生看似精確但無意義的數字。

因此若要導入，應**分動作處理**：槓鈴力量動作用 e1RM 或 Set Records，
舉重動作維持最大重量。

### 決定：先做「依幅度分級的即時 PR 通知」（2026-08-30）

**只做這一項，其餘延後。核心價值不是慶祝，是把靜默的資料錯誤變成當場的疑問。**

前提：以下三個方向之所以我們能做、大眾 App 不能，**不是技術問題，是我們容許摩擦**。
「你確定嗎？」對一個要拚留存率的 App 是負擔；對自用工具剛好合適。這是本專案相對
現有產品的結構性優勢，不只適用於本項。

#### 選定的做法

同一個機制承擔兩個角色：合理幅度的進步**安靜慶祝**，超出常態太多則**主動要求確認**。

大眾 App 的 PR 通知之所以失效，是因為它只做慶祝——初期幾乎每組都是 PR，
後期「單組容積」多加一下就觸發，久了變成壁紙沒人看，抓錯的價值歸零。
**把守門的職責加進去，通知才有理由被認真看。**

選它的理由：訊號最強、成本最低，而且是三者中**唯一直接解決本次事故**的做法
（7/31 存檔當下若有回饋，錯誤不會躺一個月）。

#### 尚待決定的設計問題

- **門檻怎麼定？** 固定比例、固定 kg、或依該動作自身的歷史級距分布。
  依自身級距最合理（見下一節的實測級距），但**新動作沒有歷史**時要有退路。
- **需要最小歷史筆數。** 第一次記錄某動作時任何重量都是 PR，不該通知。
- **「確認」的形式。** 阻擋式 `confirm()` 最簡單，但誤報時很煩；
  「先存下來、標記為待確認」較溫和，代價是要多一個狀態和一個清理它的地方。
- **顯示位置。** 存檔按鈕現有的 `Saved!` 回饋（1.5 秒）可沿用，不必新增元件。
- **只針對最大重量**。Set Records、單組容積等其他 PR 類型不在本次範圍。

#### 用使用者自己的級距判斷合理性（本項的一部分）

實測進步級距：深蹲 120→125、硬舉 130→140、划船 60→65、RDL 75→80，
全部落在 5~10 kg。大眾 App 不能假設這件事（要服務新手與機械式訓練者，那些人真的會跳很快），
我們可以。本次的 +25 kg / 33% 遠在此分布之外。

#### 延後：跨動作一致性檢查

臥推 100 真正可疑之處在於它相對**自己其他項目**失衡——深蹲 125 配臥推 75 的比例長期穩定，
突然變 100 就偏離自身基準。現有 App 完全不做跨項目檢查。

**延後的理由**：這是三者中最容易誤報的一項。個體間的力量比例差異極大，
只有「與自己的歷史比」才站得住腳，而要穩定地判斷「偏離自身基準」需要更多資料與調校。
等第一項實際運作一段時間再評估。

#### 風險：狼來了

驗證類功能的失敗模式是誤報。一旦誤報過幾次就會被忽略或關掉，**那時比沒有更糟**——
因為使用者會以為有東西在守著。

因此：從最簡單、訊號最強的一項開始，不要一次全上；門檻寧可寬鬆（漏報幾次可接受，
誤報一次的代價高得多）；並保留調整門檻的餘地。

---

## 22. 動作總覽可點擊鑽取，移除 Sparkline 區塊

**受眾**：起因用出來的、解法是推測｜三題皆**待驗證**（使用者原話：「這個是我自己猜測的改善項目，得實驗才知道」）｜對我變差：待驗證

> 狀態：**已完成**，2026-08-31 由 PR #68、#69 合併進 main（#69 為補上「回到動作列表」的回程）。
> 完整決策脈絡見 [`docs/superpowers/specs/2026-08-30-exercise-overview-drilldown-design.md`](docs/superpowers/specs/2026-08-30-exercise-overview-drilldown-design.md)。

點「最近動作總覽」的任一列，底下的 Performance Overview 圖表切換到該動作並捲動到位；
同時移除 Sparkline 區塊（省下約 240px）。

**關鍵發現**：一度考慮新增「動作詳細頁」路由，查證後發現不必要——
Performance Overview 已經是單一動作的詳細趨勢圖，只是入口在頁面最底部的下拉選單。
本次不新增路由、頁面或資料邏輯，只把表格接到既有圖表。

**移除前的樣貌與回退指引記在 design doc**（使用者要求）：Sparkline 每列 72px、
三個共約 240px 卻只顯示 6 個資訊點；顯示哪三個動作是 `slice(0, 3)` 任意取的；
而且它叫 Sparkline 卻沒有畫任何線——算出的序列只被用來取 `.length`。

---

## 23. Dashboard 區塊開關（版面隨訓練週期調整）

**受眾**：尚未問

> 由 #22 的討論分出，當時決定延後。**#22 已於 2026-08-31 上線**，
> 因此本項的前提條件已滿足——現在缺的是實際使用一段時間後的判斷。

**問題**：Dashboard 是固定的，但訓練不是。使用者會循環經過不同週期——
增肌減脂＋控制大重量、最大力量、以舉重動作為主軸——**每個週期該看的東西不一樣**。
目前的版面對所有週期都一樣，於是總有幾個區塊在當下是沒用的。

**為什麼延後**：使用者提出時同時抱怨了「區塊 UI 太肥」。拆解後發現那是兩個問題：
版面現在就壞（#22 處理），以及週期會變（本項）。

**加開關解決不了壞掉的區塊，反而會把它藏起來**——一個壞掉的區塊被加上開關之後，
就從「壞掉」變成「可以關掉」，於是永遠不會被修好。

使用者決定先修 #22，「不知道，可能要等修好以後才會知道」還需不需要開關。

**若日後要做，尚待決定**：偏好狀態存在哪（localStorage 只在本機，backend 可跨裝置）；
是純開關，還是進一步做成「訓練週期預設組合」。

---

## 24. Record 頁日期左右箭頭（±1 天）

**受眾**：用出來的｜僅我：否——想快速翻看過去某一天是通用需求｜對我變差：否

> 狀態：**已完成**，2026-09-05 由 PR #70 合併進 main。
> 完整決策脈絡見 [`docs/superpowers/specs/2026-08-31-record-date-arrows-design.md`](docs/superpowers/specs/2026-08-31-record-date-arrows-design.md)。

日期那一列改為 `‹ 2026-08-31 ›`，一個 tap ±1 天，不增加任何高度；
點日期本身仍開啟原生選擇器，供跨度較大的跳轉。

**設計要點（已定案，實作時不必重想）**：

- `shiftDays()` 從 `sessionStore.js` 的私有函式搬到 `src/utils/date.js`，
  並補上單元測試（跨月、跨年、閏年）。sessionStore 改為 import，不再各自持有一份。
- **「下一天」在 `selectedDate >= 今天` 時停用。** 本 app 是紀錄工具而非計畫工具，
  資料中未來日期為 0 筆，不擋的話連點兩下就會靜默記到明天。
  解除此限制的條件見 #20 的〈相關決定〉。
- 按鈕停用時降透明度而非隱藏，讓日期不會左右跳動。
- 無自動化測試（元件互動測不了，見 #10）；`shiftDays` 本身可完整 TDD。

**資料佐證**：484 筆中補記過去者 19 筆，**全部都是前一天**，無一超過 1 天。
因此「回到昨天」是最頻繁的日期操作。

---

## 25. 計畫 / 已執行（預先安排課表的資料模型）

**受眾**：尚未問——目前不是需求，是從 #24 的邊界條件分析中記下來的可能性

> 2026-08-31 由 #24 的討論分出。**#24 維持現狀：日期不得往未來走。**

### 起因

#24 的日期箭頭擋住了未來日期。討論時浮現一個問題：如果之後想「預先安排課表」，
是不是把箭頭放開就好？

**不是。** 使用者對本專案的描述是「紀錄訓練表現，甚至有一部分在幫我安排課表」，
所以這個方向本身是合理的——但它不是一個邊界條件，是一個資料模型問題。

### 核心問題：計畫與已執行在資料上長得一模一樣

`TrainingSession` 目前只有 `date` / `exercise` / `weight` / `reps`，
**沒有任何欄位能區分「排定要做」與「已經做完」**。

若只放行未來日期，計畫中的紀錄會被所有指標當成已完成的訓練：

| 受影響的計算 | 會發生什麼 |
|---|---|
| `exerciseOverview` 的 `maxWeight` | **顯示還沒做到的重量。** 掃全部歷史、不看日期，排一組「下週深蹲 130」就立刻變成最重 130 |
| `weeklyTrainingVolumeInfo` | **本週容積被灌水。** 排給明天（若仍在本週）的訓練今天就計入，再拿去跟 12 週平均比較，趨勢失真 |
| `exerciseOverview` 的 `recentSets` / `recentReps` | 視窗只有起點沒有終點（`session.date >= recentStart`），未來日期一律計入 |
| `exerciseOverview` 的 `streakWeeks` | **不受影響**——自當週往回數，不看未來 |

這與 #21 那筆錯誤的臥推 100 是同一種病（污染最大重量且畫面看不出來），
差別在於它會**系統性地、每次排課表都發生**。

### 範圍

- **後端 schema 變更**：`backend/src/repositories/types.ts` 的 `TrainingSession` 加狀態欄位，
  連帶要改 Mongo 與 Firestore 兩個 repository 實作以及 `LIFFService`。
  這兩個 repository **目前零測試覆蓋**（見 #4），因此實質上應排在 #4 之後。
- **每一個 getter 都要決定自己算不算計畫中的紀錄。** 這不是加一個 filter 就好——
  「本週容積」該不該顯示計畫量？「最近 14 天做了多少」呢？各自的答案可能不同。
- UI 上要能區分兩種紀錄，並提供「把計畫標記為完成」的路徑。

### 相關

- **#24**：本項是其「解除未來日期限制」的前置。在本項完成前，箭頭維持擋住。
- **#14 訓練筆記**：同樣要動 backend schema，可一併設計以免動兩次。
- **#2 動作變化通知**：若有計畫資料，通知的語意會改變（「這週排了但還沒做」是另一種狀態）。
- **#4**：後端測試覆蓋是本項的實質前置。

### 明確不做

**不要用「放開箭頭」當成本項的簡易版。** 那會讓計畫與已執行混在同一批紀錄裡，
污染所有指標且畫面上看不出來——比不做更糟。

---

## 產品原則：護城河在哪裡（2026-08-29）

> 記錄一次關於「vibe coding 讓商業邏輯容易被抄」的討論結論。這不是待辦事項，是用來判斷未來決策的依據——例如「要不要把 repo 轉 private」「要不要把邏輯藏到後端」。

**結論：護城河不是「別人抄不到」，是「別人抄到的當下就已經過期，而且他沒有能力跟上」。**

本專案的「商業邏輯」——12 週容積基準、連續週數規則、體重趨勢門檻——加起來不到 40 行算術，任何人拿 LLM 一個下午就能重寫。而這幾樣東西在 2026-08-25 那一週內就改了兩次。

真正發生的事情是：打開 Dashboard，看到一個三個月沒練的動作顯示「連續 8 週」，意識到它回答錯了問題。抄襲者抄走的是**當時那一版**，包含那個錯誤錨點——他拿到的是答案，不是發現答案錯了的能力。

### 實際存在的護城河（依重要性）

1. **資料 × 時間**：本專案的核心功能（12 週基準、趨勢、PR、連續週數）**全部需要歷史才有意義**。克隆版打開來是空的，價值為零，而且差距每天擴大。
2. **判斷的累積**：`openspec/specs/` 記行為，`docs/superpowers/specs/` 記「為什麼這樣決定、否決了什麼」。抄得到前者，抄不到後者。
3. **迭代速度的複利**：有 spec 有測試，才敢在週五晚上改掉核心語意並確認 68 個測試仍綠。vibe code 出克隆版的人改任何東西都是在賭。
4. **通路**：LINE LIFF 是一個位置，不是一個功能。

### 由此推出的具體判斷

- **不值得做**：混淆前端、把訓練分析邏輯搬到後端「以免被看到」。前端程式碼永遠是公開的，跟 repo 可見性無關。
- **該做，但理由不是保密**：把 `sessionStore` 的計算搬到後端（見 #3）。理由是**效能與多端一致**——目前 8 個 getter 要先把整個使用者的 sessions 下載到瀏覽器才能算，資料量長大後不可行。
- **真正該防的只有兩樣**：**憑證與成本**（#7 的 AI API key 必須在後端，否則是別人拿去花你的錢）、**資料**（#17）。
- **repo 可見性**：隨時可轉 private，不必現在決定。但要知道代價——本專案前端是靠 GitHub Pages 部署（`.github/workflows/deploy.yml`），Free 方案的 private repo **不能用 Pages**，需 Pro；且即使升級，Pages 站台本身仍是公開的（站台私有需 Enterprise Cloud）。轉 private 也會失去免費的 secret scanning 與 code scanning。**Dependabot alerts 不受影響。**

### 誠實的但書

有些領域的邏輯確實是護城河（專有資料訓練的模型、累積多年的定價演算法、法規遵循 know-how），但這些護城河的材料是**資料與累積的脈絡，不是程式碼**，規則仍然一致。

對單人開發者而言，「被抄」遠不是最可能的失敗方式，**沒有人在乎到願意抄**才是。花在藏程式碼上的力氣，都是沒有花在讓東西值得被抄上面。

---

## 建議執行順序

1. ~~**#15** 修正當週平均體重趨勢顯示~~ ✅ 完成（2026-08-23）
2. ~~**#11** 記住上次選的動作~~ ✅ 完成（2026-08-23）
3. **#17** 確認並鎖上 Firestore 安全規則（可能現在就是敞開的，先查 console 再說）← **下一步**
4. ~~**#4 (a)** 修掉 feature branch 部署到正式站 + PR gate~~ ✅ 完成（2026-08-29）
5. **#12 (a)(b)** 選單改從 sessions 推導 + `constants.js` 改名（純前端、無風險，做完就不會再產生新的名稱分岔）
6. **#2 + #13** 動作變化通知與動作注意事項提示（主菜；先走 brainstorming 把 #2 的規則問題定案，兩者的提示 UI 一起設計）
7. **#12 (c)** 44 筆資料清理（動到正式資料，單獨處理，建議排在 #2/#13 開工前完成）
8. **#10** 元件測試基礎設施（#4b 後端自動部署已列為暫緩，不在此序列中）
9. **#3** 資料匯出
10. **#14** 訓練筆記（需要 #4 先完成）

**#6 SKILLS** 成本近零，可隨時插隊。
**#5 MCP** 唯讀讀取已透過 `.mcp.json` 完成並實際用於 migration 與資料稽核；領域層 MCP 待評估是否有需要。
