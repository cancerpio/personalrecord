## 1. 資料層：趨勢改採 2A（完整週對完整週）

- [ ] 1.1 於 `weeklyTrainingVolumeInfo` 取「有紀錄且非當週」的完整週集合，找出「上一完整週」（最新一週）與「前期各週」（其餘）
- [ ] 1.2 歷史平均改為「前期各週容積平均」；趨勢改以「上一完整週 vs 前期各週平均」判定（維持 ±5% 門檻）
- [ ] 1.3 邊界處理：只有一個完整週時回傳趨勢 STABLE（持平）；無完整週時維持既有無資料處理
- [ ] 1.4 保留當週即時容積（partial）作為 `currentVolume` 供標頭大數字使用，不參與趨勢計算
- [ ] 1.5 更新既有 `weeklyTrainingVolumeInfo` 相關單元測試以反映新語意

## 2. 資料層：每週與當週平均體重

- [ ] 2.1 擴充 `trailing16WeekVolumeInfo`，為每一週新增 `avgBodyWeight`＝該週 `bodyWeight` 平均，無紀錄為 `null`（重用既有週分組與 `getMondayOfDate`）
- [ ] 2.2 提供「當週平均體重」＝當週 `bodyWeight` 平均，無紀錄以無資料狀態表示
- [ ] 2.3 提供「體重趨勢」＝以 2A（上一完整週 vs 前期各週平均）計算方向與變化量（kg），採建議門檻 ±0.3 kg；只有一個完整週時為持平
- [ ] 2.4 為 2.1–2.3 補上單元測試（含缺值週為 null、當週平均、體重趨勢與門檻）

## 3. 首頁標頭：雙欄配置

- [ ] 3.1 將標頭改為左右兩欄：左＝當週訓練總容積（釘左上）、右＝當週平均體重（釘右上、靠右對齊），無中間分隔線
- [ ] 3.2 每欄呈現「色塊+標題 → 大數字 → 趨勢 chip」；容積 chip 用好壞色、體重 chip 用中性灰（只標方向與 kg）
- [ ] 3.3 兩欄下方加入一行共用基準小字「趨勢基準：上一完整週 vs 前期各週平均」
- [ ] 3.4 當週無體重紀錄時，右欄以無資料狀態呈現（「尚無體重紀錄」）

## 4. 首頁圖表：16 週容積 + 體重折線（Highcharts combo）

- [ ] 4.1 以 `highcharts-vue` 重建 16 週圖：column（容積，`yAxis 0`，0 起算）＋ line（體重，`yAxis 1`，右側，`softMin/softMax`、不從 0 起算）
- [ ] 4.2 體重折線以 `null` + `connectNulls: false` 在缺值週斷開（不補 0、不內插）
- [ ] 4.3 右軸範圍策略：依非 null 體重 min/max 加邊，套用最小跨度 ≈ 8kg（防雜訊放大）與最大跨度 ≈ 50kg（防離群值撐爆）；確認左軸容積不受影響
- [ ] 4.4 當週柱以「進行中」樣式呈現（ghost／虛線外框 + 進行中標示），避免被誤讀為容積驟降
- [ ] 4.5 重建 16 週平均 `plotLine`、稀疏月份 x 軸標籤
- [ ] 4.6 tooltip 顯示週日期範圍 + 容積 + 平均體重（無體重紀錄標示為無紀錄）；提供可區分兩數列的圖例
- [ ] 4.7 tooltip 支援點擊／tap 觸發（LIFF 手機），確認觸控裝置可看到單週明細
- [ ] 4.8 空狀態：16 週皆無體重紀錄時，折線區塊顯示「記錄體重即可對照」引導，而非留白
- [ ] 4.9 套用配色（容積藍 `#007AFF`/`#0A84FF`、體重橘 `#D97706`/`#FF9F0A`）並確認淺/深色主題
- [ ] 4.10 移除原純 CSS 長條圖相關樣式與模板（由 Highcharts 取代）

## 5. 驗證

- [ ] 5.1 對照視覺預覽逐項確認外觀（標頭雙欄、雙軸圖、缺值斷線、當週強調、平均線）
- [ ] 5.2 跑過既有與新增測試，確認趨勢語意變更未破壞其他功能
- [ ] 5.3 以真實/示範資料檢查週初趨勢不再必然「下降」，並校準體重 ±0.3 kg 門檻
